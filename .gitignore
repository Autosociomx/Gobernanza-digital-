node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase config contiene apiKey real — NO commitear (hallazgo E3 Acta 006)
firebase-applet-config.json

# Carpeta de trabajo temporal de auditoría
.openclaw/

# Exportaciones de otras IAs para auditar (conversación personal, regla dura 9)
docs/interno/importaciones/
